import { useNavigate } from 'react-router-dom';

const Post = () => {
    const navigate = useNavigate();
    
 // too much troble for me, even AI can't help me my brain is deprecated
 // will be fixed later 
} 